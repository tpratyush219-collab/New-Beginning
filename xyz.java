class xyz
{
    public static void main(String a)
    { 
        int l=a.length();String s;
        char ch=' ';
        s="  "+s;
        s=s.toUpperCase();
        for(int i=0;i<=a.length();i++)
        {
           char ch=a.charAt(i);}
        if(Character WhiteSpace(ch))
           {
               System.out.print("  "+Character.toUpperCase(s.charAt(i+1)));
            }
               else
           {
               System.out.print(ch);
           }
    }
}